# Finished Tasks

## Session a5c4d — 2026-08-11 13:57
- extension.md: "Besonderes Futter als Checkbox bei Tieren ermöglichen" umgesetzt. Neues strukturiertes Datenfeld `specialFood?: boolean` am `Pet`-Entity (`src/domain.ts`, in `NewPet`/`PetUpdate` aufgenommen), durchgereicht in `src/domain/petProfile.ts` (`createPetProfile`/`updatePetProfile`, nur bei `true` gesetzt, sonst weggelassen wie bei den übrigen optionalen Feldern). Checkbox-Feld in `src/components/PetFormModal.vue` ergänzt (wiederverwendet vorhandene `.checkbox-field`-Klasse aus `SettingsPage.vue`; CSS in `src/styles.css` um Zeilenumbruch für die Hinweis-`<small>` ergänzt, damit sie unter dem Label steht statt zu überlappen).
- `src/components/CustomersPage.vue`: Tierkachel zeigt jetzt einen "Besonderes Futter"-Badge (Utensils-Icon aus `@lucide/vue`, neue CSS-Klasse `.pet-special-food-badge` analog zu `.pet-species-badge`) sowie eine neue Spalte "Besonderes Futter" (Ja/Nein) im CSV-Export (Spaltenanzahl der Leerzeile für Kunden ohne Tiere korrigiert, war zuvor bereits um eine Spalte kürzer als der Header — vorbestehende Diskrepanz, jetzt für beide Fälle konsistent 14 Spalten).
- `src/components/CheckInModal.vue`: Hinweis "Besonderes Futter" erscheint jetzt zusätzlich zu Fütterungs-/Medikationsplan etc. im Check-in-Dialog, wenn das Flag gesetzt ist.
- `src/mockData.ts`: Demodaten für Balu (`p-1`) und Nala (`p-4`) auf `specialFood: true` gesetzt, da ihre bestehenden Freitext-Hinweise ohnehin auf mitgebrachtes Futter hinweisen.
- Neuer Test in `src/domain/petProfile.test.ts` (`carries the special food flag when set and omits it when falsy`).
- Verifiziert mit `vue-tsc --noEmit` (fehlerfrei), `npm run test` (224 Tests, alle grün), `npx playwright test e2e/dashboard-search.spec.ts -g "customer|pet|Tier|Kunde" --project=desktop` (9/9 grün, 2 projektspezifisch übersprungen) sowie einem Playwright-Skript gegen den Dev-Server: Badge erscheint auf der Tierkachel, Checkbox im Bearbeiten-Modal ist für Balu vorbefüllt (checked), keine Konsolenfehler.

## Session 12edf — 2026-08-11 09:12
- Test-/Wartbarkeits-Review des bisherigen Standes: `npm run test:coverage` zeigt 95,23 % Statement-Abdeckung für die reine Logik (`src/domain`, `src/store`, `src/composables`, `src/shared`) — damit über der 80%-Schwelle. Gemäß Aufgabenstellung daher Playwright-Analyse der Seitenstruktur statt weiterer Unit-Tests.
- `npx playwright test` (Projekte `desktop` + `mobile`, 53 Tests je Projekt) zunächst mit 13 (desktop) bzw. 4+ (mobile) Fehlschlägen ausgeführt. Ursachenanalyse (Fehler-Snapshots, `git blame`) ergab mehrere echte Diskrepanzen zwischen App und Testsuite, die seit vorherigen Sessions unbemerkt geblieben waren (u. a. weil sie sich gegenseitig maskierten — ein Test schlug so früh fehl, dass spätere, unabhängige Bugs nie erreicht wurden):
  - `e2e/dashboard-search.spec.ts`: zwei Tests suchten noch nach dem Linktext "Belegung planen", der bereits in Session 02936 zu "Belegung ansehen" umbenannt wurde (bugs.md Punkt 3) — Tests waren dabei nie aktualisiert worden.
  - Ein Test erwartete den Text "100 Kund:innen" in der Kundenübersicht; die Seite zeigt und zeigte dort schon immer "100 Treffer" (`CustomersPage.vue`) — vermutlich ein Tippfehler bei der Testerstellung.
  - Zwei Tests ("shows customer pets…", "uses a master-detail flow…") klickten auf "Sofia Berger", ohne vorher zu suchen; die Kundenliste ist alphabetisch nach Nachname paginiert (5 pro Seite), Berger liegt nicht auf Seite 1.
  - Vier Tests gingen davon aus, dass `/buchungen` direkt in der Listenansicht öffnet; seit der Buchungszeitachse (`BookingsPage.vue`, `view = ref(...'calendar')`) ist die Kalenderansicht Standard. Tests klicken jetzt zuerst auf "Listenansicht".
  - Fünf Tests (inkl. des seit Session 051e0 bekannten kaputten CSV-Export-Tests) gingen davon aus, dass `/check-in-out` ohne Query-Parameter die "Geplante Anreisen" zeigt; seit Commit `1c953ce "Minor fixes"` ist "Jetzt eingecheckt" der Standard-Tab. Tests navigieren jetzt explizit mit `?view=arrivals`; ein Test wurde von `toHaveCount(0)` auf `toBeDisabled()` umgestellt, da das Datumsfeld im "Jetzt eingecheckt"-Tab nicht mehr entfernt, sondern nur deaktiviert wird. Diese Verhaltensänderung ist als offene Beobachtung in `bugs.md` vermerkt, da unklar ist, ob sie beabsichtigt war.
  - Ein zuvor durch obige Fehler nie erreichter Test enthüllte eine echte Pagination-Lücke (Buchung "Balu" liegt bei 10 Einträgen/Seite nicht auf Seite 1) sowie eine mehrdeutige Playwright-Locator-Query (`getByRole('button', {name:'Heute'})` traf sowohl den Datums-Heute-Button als auch den "Geplante Abreisen 0 heute auszuchecken"-Tab) — beide gefixt (Suchfeld-Filter bzw. `exact: true`).
  - **Echter App-Bug gefunden und gefixt**: Auf der mobilen Belegungsseite (≤680px) überlappte der nicht umbrochene Flex-Container `.occupancy-controls` den Button "Reservierung anlegen" mit der Zeitraum-Auswahl ("7/14/30 Tage") — der Button ragte unsichtbar über den Viewport hinaus und sein Klickbereich wurde vom "30 Tage"-Button abgefangen (reproduzierbar per Playwright-Fehlermeldung "element intercepts pointer events" sowie per Screenshot verifiziert). Fix in `src/styles.css`: `flex-wrap:wrap` für `.occupancy-controls` sowie volle Breite für `.range-filters` in der 680px-Media-Query, siehe `bugs.md`.
- Nach allen Fixes: `npx playwright test` komplett grün — 97 von 106 Tests bestanden (9 projektspezifisch übersprungen, 0 Fehlschläge) über beide Projekte (`desktop`, `mobile`). Zusätzlich verifiziert mit `npm run test` (223 Tests, alle grün) und `npx vue-tsc --noEmit` (fehlerfrei).
- Alle Änderungen ausschließlich in `e2e/dashboard-search.spec.ts` (Testsuite an aktuelles App-Verhalten angepasst) und `src/styles.css` (echter Mobile-Layout-Bug behoben); keine Änderungen an der Business-Logik.

## Session 181fb — 2026-08-11 08:44
- Clean-Code-/Wartbarkeits-Review des bisherigen Standes (Komponenten, Package-Struktur, Entwurfsmuster): Grundstruktur ist sauber geschichtet (`src/domain/*` = reine Validierungs-/Berechnungslogik, `src/store/*` = Selektoren/Store-Hilfsfunktionen, `src/presentation/*` = Formatierung, `usePensionStore.ts` orchestriert nur noch); Modal-Vereinheitlichung über `BaseModal.vue` ist konsequent durchgezogen. Ein bereits in Session `5fa15` notierter, aber bewusst nicht behobener Punkt wurde als kleiner Teilaspekt aufgegriffen: `src/components/RoomOccupancyModal.vue` war toter Code — eine vollständig ausgearbeitete Komponente (Zimmerliste mit Sperren/Freigeben-Aktionen über `store.setRoomOperationalStatus`), die nirgends importiert oder in der UI verlinkt war (verifiziert per `grep` über `src/` und `e2e/`). Tote Komponente gelöscht und die ausschließlich von ihr genutzten, dadurch verwaisten CSS-Regeln aus `src/styles.css` entfernt (`.room-modal`, `.room-modal-icon`, `.room-list` inkl. Kindselektoren, `.room-status-actions` inkl. Kindselektoren, `.status-badge.available`, `.status-badge.full`); die weiterhin genutzte Basisregel `.status-badge` sowie `.status-badge.locked` (siehe `OccupancyPage.vue`) blieben erhalten.
- Verifiziert mit `vue-tsc --noEmit` (fehlerfrei) und `npm run test` (223 Tests, alle grün). Anschließende `grep`-Suche bestätigt: keine verbleibenden Referenzen auf `RoomOccupancyModal`, `.room-modal-icon`, `.room-list`, `.room-status-actions`, `.status-badge.available` oder `.status-badge.full` im gesamten Projekt.

## Session 5fa15 — 2026-08-11 08:41
- extension.md: "Gleiche color-concept.md mit den tatsächlichen Farben ab und fixe sie ggf." — die im Dokument selbst unter "Beobachtete Inkonsistenzen" gelisteten Punkte wurden im Code behoben:
  - Naming-Inkonsistenz `mid`/`medium` in der Belegungsübersicht vereinheitlicht: `OccupancyPage.vue` (`levelClass`) und `src/styles.css` (`.occupancy-rate.mid` → `.medium`, `.mobile-occupancy-day.mid` → `.medium`) nutzen jetzt durchgängig `medium` (passend zum `OccupancyLevel`-Typ in `src/domain.ts`).
  - Echter Farb-/Status-Bug behoben: Die Klasse `.status-badge.full` wurde sowohl für ein gesperrtes (Wartung) als auch für ein voll belegtes Zimmer verwendet und dadurch farblich identisch (Gold) dargestellt — obwohl "Ausgebucht" laut Farbkonzept eigentlich Blau ist. Neue Klasse `.status-badge.locked` (Grau, `#e7e2dc`/`#8b847e`, passend zur "Gesperrt"-Zeile in `color-concept.md`) ergänzt und in `OccupancyPage.vue` (Zimmer-Label) sowie `RoomOccupancyModal.vue` für den Wartungsstatus verwendet; `.status-badge.full` bleibt nur noch für "Voll belegt" reserviert.
  - Mobile Tagesansicht (`OccupancyPage.vue`, `.mobile-occupancy-day`): "Geschlossen" (Schließzeit) nutzte dieselbe CSS-Klasse `maintenance` wie "Gesperrt" (Wartung) und war dadurch farblich nicht unterscheidbar. Neue Klasse `.mobile-room-state.closed` (Beige/Rostrot `#f7ece8`/`#9a4a35`, passend zu `.occupancy-cell.closed`) ergänzt und für den Geschlossen-Fall verwendet.
  - Fehlender Eintrag "Geschlossen" in der Legende der Desktop-Belegungsübersicht ergänzt (`.legend-swatch.closed`, gleiche Farbe wie `.occupancy-cell.closed`).
  - `RequestsPage.vue`: Der Status "accepted" im Anfragenverlauf nutzte zweckentfremdet `.booking-status.checked-in` (Petrol). Neue Klasse `.booking-status.accepted` mit der "Erfolg"-Farbe aus `color-concept.md` (`#e7f2eb`/`#315f48`) ergänzt und verwendet, sodass "Angenommen" farblich von "Eingecheckt" unterscheidbar ist.
  - Beobachtung (nicht verändert, da außerhalb des Task-Scopes): `src/components/RoomOccupancyModal.vue` wird von keiner anderen Datei importiert/verwendet (verifiziert per `grep`) — es gibt aktuell keinen UI-Zugang, um einen Raum über die App tatsächlich in den Wartungsstatus zu versetzen. Die Farbkorrektur wurde trotzdem in der Datei vorgenommen, damit sie bei künftiger Wiederverwendung konsistent ist.
  - Verifiziert mit `vue-tsc --noEmit` (fehlerfrei), `npm run test` (223 Tests, alle grün) sowie einem Playwright-Skript gegen den Dev-Server: Zimmerstatus temporär auf `maintenance` sowie eine Schließzeit temporär in `src/mockData.ts` gesetzt, Screenshots von `/belegung` (Desktop 1400px und Mobile 500px) geprüft — "Gesperrt" erscheint grau, "Geschlossen" beige, "Ausgebucht"/"Teilbelegt" wie zuvor unverändert; `/anfragen`-Verlauf zeigt "Angenommen" in Grün statt Petrol. Keine Konsolenfehler. Testdaten in `src/mockData.ts` anschließend wieder zurückgesetzt (nicht Teil des Commits).

## Session 051e0 — 2026-08-11
- extension.md: "Offene Anfragen" von Inline-Formular auf Modal umgebaut. Neue Komponente `src/components/RequestAssignModal.vue` (basiert auf `BaseModal`, analog `DeclineRequestModal.vue`) enthält jetzt die Kunden-/Zimmerzuordnung (inkl. Verfügbarkeitshinweis, Kunden-Match-Hinweis und Zimmerauslastungs-Hinweis) für eine einzelne Anfrage und ruft beim Bestätigen `store.acceptRequest(...)` auf. In `src/components/RequestsPage.vue` wurden die bisherigen Inline-`<select>`-Felder pro Anfragekarte (`roomSelection`/`customerSelection`-Reactive-Maps, `requestError`, `accept()`) entfernt; die Karte zeigt nur noch Zusammenfassung + Verfügbarkeit sowie die Buttons "Annehmen" (öffnet jetzt `RequestAssignModal`) und "Ablehnen" (unverändert `DeclineRequestModal`). Verwaiste CSS-Regeln `.request-actions select` und `.request-actions .form-error` aus `src/styles.css` entfernt, neue Regeln `.request-assign-modal`, `.request-assign-form` ergänzt.
- `e2e/dashboard-search.spec.ts` an die Modal-Struktur angepasst: Tests, die zuvor direkt auf die Kunden-/Zimmer-Selects der Anfragekarte zugriffen (`Kunde für Anfrage von …`, `Zimmer für Anfrage von …`), klicken jetzt zuerst auf "Annehmen", um das Modal zu öffnen, und greifen dort über `dialog.getByLabel('Kunde zuordnen')`/`dialog.getByLabel('Zimmer wählen')` zu.
- Verifiziert mit `vue-tsc --noEmit` (fehlerfrei), `npm run test` (223 Tests, alle grün) und `npx playwright test e2e/dashboard-search.spec.ts -g "request|Anfrage|closure" --project=desktop` (6/6 grün). Zusätzlich per Playwright-Skript gegen den Dev-Server verifiziert: keine Inline-Selects mehr in `.request-actions`, Modal öffnet mit beiden Auswahlfeldern, schließt per Escape — keine Konsolenfehler. Der bereits vorher fehlschlagende CSV-Export-Test (`exports the data behind every list as CSV`) wurde per `git stash` als vorbestehendes, von dieser Änderung unabhängiges Problem verifiziert.

## Session c36ee — 2026-08-11
- extension.md: "Schließzeit anlegen" von Inline-Formular auf Modal umgebaut (inkl. Bearbeiten). Neue Komponente `src/components/ClosureFormModal.vue` (analog `CustomerFormModal.vue`, basiert auf `BaseModal`, ein Formular für Anlegen und Bearbeiten je nach `mode`-Prop) ersetzt das bisherige Inline-`<form class="closure-form">` in `src/components/OccupancyPage.vue`. Neuer Store-Vorgang `updatePensionClosure(closureId, input)` in `src/usePensionStore.ts` (validiert wie `createPensionClosure` über `isValidPensionClosure`) sowie neuer Domain-Typ `PensionClosureUpdate` in `src/domain.ts`. Liste der Schließzeiten hat jetzt pro Eintrag einen Bearbeiten-Button (Pencil-Icon) neben dem bestehenden Löschen-Button; der Panel-Header hat einen "Schließzeit anlegen"-Button statt des Inline-Formulars. Verwaiste CSS-Regel `.closure-form` (inkl. Media-Query) in `src/styles.css` entfernt, neue Regeln `.closure-form-modal`, `.closure-form-modal-form`, `.closure-actions` ergänzt.
- `e2e/dashboard-search.spec.ts` angepasst: beide Closure-Tests klicken jetzt zuerst auf "Schließzeit anlegen", um das Modal zu öffnen, bevor die Formularfelder befüllt werden.
- Verifiziert mit `vue-tsc --noEmit` (fehlerfrei), `npm run test` (223 Tests, alle grün) und `npx playwright test e2e/dashboard-search.spec.ts -g closure --project=desktop` (beide Tests grün). Die beiden `mobile`-Varianten dieser Tests schlagen bereits auf unverändertem `main` fehl (verifiziert per `git stash`) an einem vorbestehenden Klick-Interception-Problem durch die lange, ungefilterte Liste in `.mobile-occupancy` bei 30-Tage-Ansicht und sind nicht auf diese Änderung zurückzuführen. Zusätzlich per Playwright-Skript gegen den Dev-Server verifiziert: Anlegen-Modal öffnen/speichern, Bearbeiten-Modal mit vorbefüllten Werten, Speichern aktualisiert die Liste — keine Konsolenfehler.

## Session 46702 — 2026-08-10
- extension.md: "Neue Buchung" von Inline-Formular auf Modal umgebaut (inkl. Bearbeiten). Neue Komponente `src/components/BookingFormModal.vue` (basiert auf `BaseModal`, ein Formular für Anlegen (mit Kunden-/Tierauswahl über `useReservationDraft`, analog `OccupancyReservationModal.vue`) und Bearbeiten (nur Zimmer/Zeitraum/Hinweis, wie zuvor) je nach `mode`-Prop) ersetzt die bisherigen Inline-Panels `<section v-if="formOpen">` und `<section v-if="bookingBeingEdited">` sowie zugehörigen State/Funktionen (`formOpen`, `error`, `editDraft`, `editError`, `useReservationDraft`-Aufruf, `submit`, `submitEdit`, `selectPets`, `editRoomAvailability`, `reservationPricePreview`, `editPricePreview`) in `src/components/BookingsPage.vue`. Die bestehende URL-Deep-Link-Logik (`?bookingId=…&edit=true` von Check-in/out, `?customerId=…` vom Kundenprofil) bleibt erhalten und öffnet das Modal jetzt über `bookingModalMode`/`bookingModalTarget`/`bookingModalPresetCustomerId`. Verwaiste CSS-Regeln (`.booking-form-panel`, `.booking-form` inkl. Media-Queries, `.edit-booking-panel`, `.edit-booking-form`) aus `src/styles.css` entfernt; neue Regel `.booking-form-modal` (Breite) sowie `textarea`-Styling für `.occupancy-reservation-form` ergänzt (jetzt von Occupancy- und Booking-Modal gemeinsam genutzt).
- `e2e/dashboard-search.spec.ts` an die Modal-Struktur angepasst (Selektoren `.booking-form`/`.booking-form-panel`/`.edit-booking-panel`/`.edit-booking-form` durch `page.getByRole('dialog')`/`.booking-form-modal` ersetzt).
- Verifiziert mit `vue-tsc --noEmit` (fehlerfrei), `npm run test` (223 Tests, alle grün) und `npx playwright test e2e/dashboard-search.spec.ts` — die betroffenen Buchungs-Tests laufen durch; 4 andere Tests (Pagination der Listenansicht, Checkout-Test, "opens a planned booking for editing directly from the booking list", "creates a booking through its customer and pet relationship") scheitern bereits auf dem unveränderten `main`-Stand (verifiziert per `git stash`) an einem vorbestehenden Problem (Buchungsseite startet standardmäßig in der Kalenderansicht, Tests wechseln nicht in die Listenansicht) und sind nicht auf diese Änderung zurückzuführen.

## Session ec823 — 2026-08-10
- extension.md: Funktion "Notfallkontakt hinterlegen" komplett entfernt (war eine optionale Zweitkontakt-Angabe pro Kunde für Notfälle während eines Aufenthalts). Entfernt: `EmergencyContact`-Typ und `emergencyContact`-Feld aus `src/domain.ts` (`Customer`), Domain-Helfer `src/domain/emergencyContact.ts` inkl. Test gelöscht, Nutzung in `src/domain/customerProfile.ts` (`createCustomerProfile`) entfernt, Store-Funktionen `updateCustomerEmergencyContact`/`removeCustomerEmergencyContact` aus `src/usePensionStore.ts` entfernt, UI-Abschnitt "Notfallkontakt" (Anzeige/Anlegen/Bearbeiten/Entfernen) aus `src/components/CustomersPage.vue` entfernt (inkl. CSV-Export-Spalte), Anzeige im Check-in-Modal (`src/components/CheckInModal.vue`) entfernt, Mockdaten in `src/mockData.ts` bereinigt, verwaiste CSS-Regeln (`.emergency-contact-section`, `.emergency-contact-value*`, `.empty-contact`, `.emergency-contact-form*`) aus `src/styles.css` entfernt (generische, wiederverwendete Klassen wie `.emergency-contact-actions`, `.emergency-contact-removal`, `.danger-button`, `.danger-text-button` bleiben erhalten, da sie auch für die Kunden-/Tier-Löschbestätigungen genutzt werden). Zugehörige Tests in `src/domain/customerProfile.test.ts` und `src/usePensionStore.test.ts` angepasst bzw. entfernt.
- Verifiziert mit `vue-tsc --noEmit` (fehlerfrei) und `npm run test` (223 Tests, alle grün).

## Session c357b — 2026-08-10
- extension.md: "Tier anlegen" von Inline-Formular auf Modal umgebaut (inkl. Bearbeiten). Neue Komponente `src/components/PetFormModal.vue` (basiert auf `BaseModal`, analog zu `CustomerFormModal.vue`; Tierart-Auswahl nur im `create`-Modus, da Tierart bei Bearbeitung stabil bleibt) ersetzt die bisherigen Inline-Formulare und zugehörigen State/Funktionen (`petFormOpen`, `petFormError`, `newPet`, `editingPetId`, `petEditError`, `editedPet`, `submitPet`, `submitPetEdit`, `closePetForm`, `openPetEdit`/`closePetEdit`) in `src/components/CustomersPage.vue`. Verwaist gewordene CSS-Regeln `.pet-create-form`/`.pet-edit-form`/`.pet-edit-actions` in `src/styles.css` entfernt und durch `.pet-form-modal`/`.pet-form` ersetzt.
- Verifiziert mit `vue-tsc --noEmit`, `npm run build`, `npm run test` (227 Tests) sowie einem Playwright-Skript gegen den Dev-Server: Anlegen-Modal öffnen/speichern (neues Tier erscheint in der Kachel-Liste), Bearbeiten-Modal mit vorbefüllten Werten (Name/Rasse), Speichern aktualisiert die Kachel — keine Konsolenfehler.

## Session 47e9f — 2026-08-10
- extension.md: "Kund:in anlegen" von Inline-Formular auf Modal umgebaut (inkl. Bearbeiten). Neue Komponente `src/components/CustomerFormModal.vue` (basiert auf `BaseModal`, ein Formular für Anlegen und Bearbeiten je nach `mode`-Prop) ersetzt die bisherigen Inline-Formulare und zugehörigen State/Funktionen (`customerFormOpen`, `customerEditOpen`, `newCustomer`, `editedCustomer`, `submitCustomer`, `submitCustomerEdit`, …) in `src/components/CustomersPage.vue`. Formularstile in `src/styles.css` (`.customer-create-form`, neue Klasse `.customer-form-modal`) auf vertikales Modal-Layout umgestellt.
- Verifiziert mit `vue-tsc --noEmit`, `npm run test` (227 Tests) sowie einem Playwright-Skript gegen den Dev-Server: Anlegen-Modal öffnen/speichern, Bearbeiten-Modal mit vorbefüllten Werten, Schließen per Escape — keine Konsolenfehler.

## Session 02936 — 2026-08-10
- bugs.md: Dashboard-Kennzahlenkarten – Pfeil-Icon (metric-arrow) war bei umbrochenem Text auf mittleren Screens (~1024px) vertikal mittig positioniert und überlappte den umbrochenen Fließtext. Jetzt fest oben ausgerichtet (`src/styles.css`, `.metric-arrow`), sodass er bei jeder Zeilenzahl neben dem Titel bleibt.
- bugs.md: "Anzahl Kunden"-Badge in der Kopfzeile von Kunden & Tiere entfernt (`src/components/CustomersPage.vue`), inkl. Entfernen des nun ungenutzten `Users`-Icon-Imports.
- bugs.md: Dashboard-Belegungskarte – Button "Belegung planen" entfernt; Panel-Titel ist jetzt "Belegung ansehen" und selbst ein Link auf `/belegung` (`src/App.vue`, neue CSS-Klasse `.panel-title-link` in `src/styles.css`, ungenutzte `.occupancy-actions`-Regel entfernt).
- Alle Änderungen mit `vue-tsc --noEmit` und `npm run test` (227 Tests) verifiziert; Layout-Fixes zusätzlich per Playwright-Screenshot bei 1024px und 1440px Breite geprüft.

## Session a1b2c — 2026-08-10
- extension.md: Das Logo wurde nur als orange Fläche angezeigt, da die `logo.svg` Datei fehlerhaft exportiert war (fehlende Details und extrem viele Pfadpunkte).
- Neue, optimierte `public/assets/logo.svg` erstellt, die Pfoten-Elemente, Hausform, Dachlinie, Tür und Fenster enthält.
- Logo-Einbindung in `src/App.vue` und `src/components/IntroPage.vue` angepasst: `style="color: white"` hinzugefügt, damit das Logo weiß auf dem orangen `brand-mark` Hintergrund erscheint.
- Die korrigierte `logo.svg` wurde auch nach `ui_ux_design/assets/logo.svg` kopiert, um die Design-Assets synchron zu halten.
- `LogoIcon.vue` Komponente erstellt und in `App.vue` sowie `IntroPage.vue` integriert. Das Logo in der `IntroPage.vue` hat nun keine Drehung mehr und verwendet die Primärfarbe direkt.
