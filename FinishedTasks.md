# Erledigte Aufgaben

- 2026-08-17 | Session b3d1a | Vier offene `todo.md`-Punkte (#18–#21) abgeschlossen: (1) Modulo-7-Fotoverteilung für generierte Zusatztiere war bereits vorhanden (`mockData.ts`, `index % 7 !== 0`) und nur noch als erledigt markiert; (2) Login-Seite (`LoginPage.vue`) erhält jetzt einen Demo-Hinweis im lila Demodaten-Farbschema (`#f3efff`/`#5b4a8c`/`#b6a1e5`, konsistent mit `DemoDataControl.vue`), der erklärt, dass beliebige Zugangsdaten funktionieren; (3) Tarifkonditionen-Spalte auf `/request-demo` (`RequestDemoPage.vue`) zeigt jetzt alle konfigurierten Tarife samt Preisstufen an statt nur den im Formular ausgewählten; (4) Dashboard-Kennzahlenkarten (`App.vue`) nutzen auf der kleinsten Auflösung `grid-cols-2` statt `grid-cols-1`, sodass zwei Karten pro Zeile stehen. Produktionsbuild (`vue-tsc -b && vite build`) und alle 217 Vitest-Tests erfolgreich.
- 2026-08-17 | Session df77e | Tierfoto-Mockdaten für die automatisch generierten Zusatztiere (`additionalPets` in `mockData.ts`) ergänzt: Statt bisher komplett ohne Foto zu bleiben, erhält jetzt fast jedes generierte Tier per `index % 7 !== 0` ein Vorschaubild aus `public/assets` (Variante `(index % 6) + 1`, passend zur Tierart Hund/Katze); nur jedes siebte Tier bleibt bewusst ohne Foto, damit der Fallback-Avatar in Listen und Detailansicht weiterhin sichtbar bleibt. Typecheck (`vue-tsc --noEmit`) und alle 217 Vitest-Tests erfolgreich.

- 2026-08-17 | Session e6779 | Zwei Drawer-Bugs behoben (`tasks/bugs.md` #6 und #7): Das „Demodaten“-Menü im Header (`DemoDataControl.vue`, `z-40`) lag über der mobilen Navigations-Schublade (`z-20`), sodass dessen Buttons sichtbar in den geöffneten Drawer ragten – Drawer (`App.vue`) und dessen Abdunkelungs-Overlay wurden auf `z-[42]` bzw. `z-[41]` angehoben, sodass der Drawer beim Öffnen wieder ganz oben liegt. Zusätzlich wurde der Schließen-Button (X) aus einer absoluten Positionierung, die bei längeren Titeln in das „Pro“ von „Tierpension Pro“ hineinragte, in eine Flex-Zeile (`justify-between`) neben dem Titel verschoben, sodass Titel und Button sich nie überlappen. Produktionsbuild sowie alle 217 Vitest-Tests erfolgreich.

- 2026-08-16 21:20 | Session 3d41f | Preisübersicht auf der öffentlichen Anfrageseite (`/request-demo`) von einer terminbezogenen Berechnung auf eine reine Auflistung der Tarifkonditionen umgestellt: Statt Preis pro Tier über Anreise-/Abreisedatum zu berechnen, zeigt die Spalte jetzt direkt die Preisstufen (`tier.startsAtAnimal`, `tier.amountCents`) des gewählten Tarifs als „X. Tier · Y € / Nacht"-Liste, unabhängig vom konkreten Zeitraum oder der Anzahl eingetragener Tiere. `calculateTariffReservationPrice` wird auf dieser Seite nicht mehr verwendet. Produktionsbuild und alle 217 Vitest-Tests erfolgreich.

- 2026-08-16 22:20 | Session b9e6d | Tierfoto-Mockdaten von generierten SVG-Platzhaltern auf echte Bilddateien aus `public/assets` (`dog1.png`–`dog6.png`, `cat1.png`–`cat6.png`) umgestellt (`petPhoto()` in `mockData.ts`); zusätzliche Demo-Tiere (Oskar, Loki, Minou) haben jetzt ebenfalls ein Foto, um mehr Varianten zu zeigen. Das Vorschaubild in der Tier-Detailkarte (Kunden & Tiere → ausgewählter Kunde → „Tiere“) wurde von 32px auf 80px vergrößert (abgerundetes Rechteck statt kleinem Icon-Chip); die Kundenliste selbst zeigt weiterhin keine Fotos. Typecheck und alle 217 Vitest-Tests erfolgreich.
- 2026-08-16 22:05 | Session bfc80 | Tierfotos in die Tier-Detailansicht (Kunden & Tiere → ausgewählter Kunde → „Tiere“-Bereich) eingebaut, in der Kundenliste bleiben nur die bisherigen Textnamen (keine Fotos). `Pet.photoUrl` ist ein neues optionales Domänenfeld; `PetFormModal.vue` bietet dafür einen Foto-Upload per Dateiauswahl (FileReader → Data-URL, kein externer Bild-Host) mit Vorschau und „Foto entfernen“. Ohne Foto zeigt die Detailkarte weiterhin den bisherigen Icon-Avatar als Fallback. Mockdaten: mehrere Demo-Tiere haben jetzt eine lokal generierte, illustrative Platzhalter-Foto-Data-URI (`petPhoto()` in `mockData.ts`), einige bleiben bewusst ohne Foto, um den Fallback-Zustand zu zeigen. Zusätzlich den veralteten Hinweis „Ein ausführlichere Tarifkonfiguration ist noch in der Entwicklung…“ auf der Einstellungsseite entfernt (Tarife sind längst produktiv). Typecheck und alle 217 Vitest-Tests erfolgreich; Feature manuell im laufenden Dev-Server per Playwright-Skript verifiziert (Foto-Anzeige, Upload, Speichern, Fallback-Icon).

- 2026-08-16 21:35 | Session 92345 | Auf der öffentlichen Anfrageseite (`/request-demo`) unten einen Link „← Zurück zur Demo Startseite“ ergänzt, der zur Intro-Route (`/`) zurückführt. Typecheck und alle 217 Vitest-Tests erfolgreich.

- 2026-08-16 21:20 | Session f1fd5 | Preisübersicht auf der öffentlichen Anfrageseite (`/request-demo`) ergänzt: Neben dem Anfrageformular erscheint jetzt eine eigene, auf größeren Breiten sticky positionierte Spalte, die den voraussichtlichen Preis pro Tier sowie den Gesamtbetrag anzeigt, sobald Tarif, Anreise- und Abreisedatum gültig ausgefüllt sind (`calculateTariffReservationPrice`, `formatEuroCents`). Solange die Angaben unvollständig sind, zeigt die Spalte einen Hinweistext statt einer Berechnung. Typecheck, Produktionsbuild und alle 217 Vitest-Tests erfolgreich.

- 2026-08-16 20:52 | Session b95a5 | Pauschalen Tagespreis pro Tierart entfernt: Das alte Flatrate-Preismodell (`PensionSettings.dailyPetRates`, `DailyPetRate`, `calculateStayPrice`/`calculateReservationPrice`) wurde vollständig aus Domänenmodell, Store, Mockdaten und Einstellungen-UI entfernt. Der Check-out berechnet den Preis jetzt ausschließlich über den 1:1 mit dem Zimmer verknüpften Preisstaffeltarif; für Alt-Buchungen ohne explizites `tariffId` greift als Fallback der Tarif des Zimmers (`room.tariffId`), sodass auch historische Demo-Buchungen weiterhin korrekt abgerechnet werden. Die Checkout-Preisvorschau im Dashboard (`App.vue`) nutzt dafür die neue Store-Funktion `previewCheckoutPrice`, die dieselbe Tariflogik wie der eigentliche Check-out verwendet. Die Einstellungsseite „Tarife" zeigt keine Eingabefelder für „Hund/Katze pro Tag" mehr. Tests für die entfernte Preisformel wurden durch Tests für `calculateTariffReservationPrice` ersetzt. Typecheck, Produktionsbuild und alle 217 Vitest-Tests erfolgreich; die einzige fehlschlagende Playwright-Suite (`settings-draft-actions.spec.ts`) ist ein bereits vor dieser Änderung bestehender, unabhängiger Testfehler.

- 2026-08-16 19:10 | Session 5bbd6 | Tarifauswahl in der öffentlichen Anfrageseite (`/request-demo`) ergänzt: Kundinnen und Kunden wählen jetzt beim Absenden einer externen Anfrage explizit einen der konfigurierten Preisstaffeltarife (`BookingRequest.tariffId`). Bei der Zuordnung im internen Anfragen-Bereich werden ausschließlich Zimmer angeboten, die 1:1 mit dem angefragten Tarif verknüpft sind (Store-Guard in `acceptRequest` lehnt abweichende Tarif/Zimmer-Kombinationen ab); die angenommene Reservierung übernimmt den Zimmer-Tarif, sodass die Preisberechnung beim Check-out korrekt greift. Anfragenliste und Zuordnungsdialog zeigen den gewünschten Tarif zur besseren Übersicht an. Demodaten (offene Anfragen) sind entsprechend mit passenden Tarifen hinterlegt. Typecheck sowie alle 223 Tests erfolgreich.

- 2026-08-16 18:35 | Session 05e32 | Tarifmodell für Unterbringungen umgesetzt: Jedes Zimmer ist nun 1:1 fest mit genau einem Preisstaffeltarif verknüpft (`Room.tariffId`). Die Zimmerverwaltung in den Einstellungen bietet dafür eine Tarifauswahl beim Anlegen und Bearbeiten und zeigt den zugeordneten Tarif in der Zimmerliste an. Store-seitig wird sowohl beim Anlegen/Ändern eines Zimmers als auch beim Speichern der Einstellungen geprüft, dass der referenzierte Tarif existiert bzw. nicht entfernt werden kann, solange ihm noch eine Unterbringung zugeordnet ist. Demodaten sind entsprechend verknüpft (Katzenloft 2 → Einzelzimmer, übrige Zimmer → Standardzimmer). Produktionsbuild sowie alle 223 Tests erfolgreich.

- 2026-08-16 18:01 | Session 5ed26 | Neues Preisstaffeltarifmodell in Buchungen umgesetzt: Neue Einzel- und Sammelbuchungen speichern den ausgewählten Tarif als Mock-Entitätsreferenz. Die Buchungsmaske bietet die Tarifwahl und berechnet die Vorschau positionsgerecht für alle Tiere und Nächte. Beim Check-out werden tarifbasierte Einzelanteile als unveränderlicher Snapshot gespeichert; historische Mock-Buchungen bleiben über den bisherigen Tagespreis kompatibel.

- 2026-08-16 17:58 | Session ebdfb | Preisstaffeltarife in den Einstellungen implementiert: eigenständige Mock-Entitäten mit Namen und Preisstufen, inklusive Demotarifen „Einzelzimmer“ und „Standardzimmer“. Die Tarifverwaltung erlaubt Anlegen, Umbenennen sowie Hinzufügen und Entfernen von Preisstufen. Die Domänenvalidierung erzwingt die erste Stufe ab Tier 1, strikt aufsteigende Tierpositionen und positive Cent-Beträge; die Berechnung bildet Staffelpreise pro Nacht und über mehrere Nächte ab. Buchungen bleiben entsprechend der Aufgabenabgrenzung unverändert. Produktionsbuild sowie 221 Tests erfolgreich.

- 2026-08-16 16:07 | Session abaeb | Anfrageannahme um eine explizite Tierzuordnung erweitert: Mitarbeitende können nun für jedes angefragte Tier ein passendes bestehendes Tierprofil des gewählten Kunden wählen oder ein neues Mock-Tierprofil anlegen. Die Store-Logik validiert Besitzer und Tierart, verhindert Doppelzuordnungen und erzeugt für mehrere neue Tiere eindeutige Entitäten samt gemeinsamer Reservierung. Die öffentliche Anfrageseite persistiert ihre validierten `NewBookingRequest`-Mock-Entitäten bereits in derselben Warteschlange; beide offenen Aufgaben in `tasks/todo.md` sind als erledigt markiert. Produktionsbuild erfolgreich; die Tests enthalten einen bereits bestehenden, unabhängigen Erwartungsfehler beim Namen der Demopension.

- 2026-08-16 16:03 | Session e8e3c | Öffentliche Anfrageseite für mehrere Tiere erweitert: Das Formular verwaltet dynamische Tier-Entitäten; Validierung, Zimmerverfügbarkeit und Kapazitätsprüfung berücksichtigen die gesamte Gruppe. Beim Annehmen entstehen einzelne Aufenthalte in einer gemeinsamen Mock-Reservierung. Die Anfragenübersicht, Zuordnung und der CSV-Export zeigen alle Tiere.
- 2026-08-16 16:03 | Session e8e3c | Kopfbereich der öffentlichen Anfrageseite bereinigt: Das nicht benötigte Tier-Badge wurde entfernt.

- 2026-08-16 15:53 | Session ffdb0 | Check-in-/out-Verlauf stabilisiert: Für die optionale Rückgängig-Aktion bleibt nun in jeder Verlaufszeile ein fester Aktionsslot reserviert; der Buchungslink verschiebt das Layout nicht mehr.
- 2026-08-16 15:53 | Session ffdb0 | Dashboard-Kennzahlen für mittlere Auflösungen angepasst: Die Symbolfläche steht jetzt bis zur großen Breakpoint-Stufe in einer eigenen Zeile und die Karten bleiben lesbar.
- 2026-08-16 15:53 | Session ffdb0 | Öffentliche Anfragenseite unter `/request-demo` umgesetzt: Das einbettbare, eigenständige Formular erfasst Kontakt-, Tier- und Termindaten und speichert validierte `NewBookingRequest`-Mock-Entitäten in der bestehenden Anfrage-Warteschlange. In den Einstellungen ist der persönliche Link neben der Aktivierung hinterlegt. Build und 214 Tests erfolgreich.

- 2026-08-16 15:39 | Session 638cb | Architektur der gerouteten Hauptseiten umgesetzt: `src/view` angelegt und Intro, Buchungen, Kunden, Belegung, Check-in/-out, Anfragen, Einstellungen sowie Konto aus `src/components` dorthin verschoben. Alle App- und Komponentenimporte angepasst; Router und Referenzen geprüft. Build sowie 214 Tests erfolgreich.

- 2026-08-16 16:05 | Session b45cc | Registrierungsprozess Schritt 3 umgesetzt: Route `/register/pension`, Validierung und Abschlussseite für Benutzer- sowie Pensionsdaten ergänzt. Der eigenständige Mock-Onboarding-Datensatz speichert Namen, Pensionsadresse und Abschlusszeitpunkt, ohne das bestehende Demokonto zu verändern.

- 2026-08-16 16:00 | Session f1f8e | Registrierungsprozess für E-Mail und Bestätigungscode umgesetzt: eigene Mock-Registrierungsentität mit Reset-Verhalten, Anmeldelink sowie Routen `/register` und `/register/verify`; E-Mail und Code können über URL-Parameter vorausgefüllt werden.

- 2026-08-16 15:36 | Session 2a340 | Demo-Anmeldung mit eigenem Mock-Sitzungsmodell umgesetzt: Start und Daten-Reset melden automatisch Robin Muster an; beliebige Login-Eingaben führen als Robin Muster zum Dashboard, inklusive Hinweis und Logout-Zustand.

- 2026-08-16 15:34 | Session a38f5 | Anmeldeseite und Profilmenü umgesetzt: Das Profilbild öffnet nun ein Menü zu den Kontoeinstellungen oder zur Abmeldung; Abmelden führt zur neuen Anmeldeseite.

- 2026-08-16 15:33 | Session fa763 | Dashboard-Kennzahlen für kleine Auflösungen angepasst: Das Symbol steht nun in einer eigenen Zeile oberhalb der Kartendetails.
- 2026-08-16 15:33 | Session f9231 | Mobile Navigation auf kleinen Auflösungen repariert: Der geöffnete Drawer überschreibt seine ausgeblendeten Responsive-Standardwerte wieder korrekt.
