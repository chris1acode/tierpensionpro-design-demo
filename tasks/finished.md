- Introduced `AppContainer.vue` (Tailwind-based page wrapper) and replaced all 9 raw `<main>` page roots with it, removing the custom `main { max-width:1280px; margin:auto; padding:... }` rule and its mobile `@media` override from `styles.css` — a2cd1, 2026-08-15
- Rebuilt `IntroPage.vue` (the landing/design-demo page, which had never been migrated and still had its own fully custom `<style scoped>` block incl. a `@media(max-width:480px)` override) entirely with Tailwind utility classes (`min-[480px]:` variant replacing the media query), inlining a local eyebrow-label style directly on the `<p>` instead of relying on the global `.eyebrow` class; verified via live Playwright screenshots at desktop (1280px) and mobile (390px) — 658cf, 2026-08-15 20:52
- Rebuilt `ToastRegion.vue` fully with Tailwind utility classes (incl. Tailwind's grid system and `sm:` variant instead of a media query) and Vue's `enter/leave-*-class` transition props, removing the `.toast-region`/`.toast`/`.toast-dismiss`/`.toast-enter-*`/`.toast-leave-*` rules and the related mobile override from `styles.css` — 36c54, 2026-08-15 18:40
- Introduced `AppMetricCard.vue` (Tailwind-based, prop-driven) and replaced the 8 duplicated dashboard metric-card `RouterLink` blocks in `App.vue` with it, switching the `.metrics`/`.metrics.three-cols` wrapper to Tailwind's grid system (`grid-cols-1 sm:grid-cols-3/4`) instead of a media query, and removing the now-dead `.metrics`/`.metric-card`/`.metric-arrow`/`.metrics small/strong/p` rules from `styles.css` (kept `.metric-icon`/`.orange`/`.teal`/`.rose` since `CheckInOutPage.vue` still uses them directly) — 50d4d, 2026-08-15 19:20
- Introduced `AppButton.vue` (Tailwind-based, variant-driven: `primary`/`secondary`/`danger`/`link`/`text`, renders as `<button>` or `RouterLink`) and replaced all 55 raw `.primary-button`/`.secondary-button`/`.link-button`/`.text-button`/`.danger-button` usages across 23 components with it, preserving every contextual color/size/mobile override found via cascade analysis and live before/after screenshots (e.g. `.section-title .text-button` → `primary-dark`, `.demo-menu .secondary-button` wrapping style, `.pet-removal-confirmation .danger-button` compact size, `.bookings-page .page-heading>.primary-button` mobile full-width); removed the now-fully-dead `.primary-button`/`.secondary-button`/`.outline-button`/`.accent-button`/`.link-button`/`.text-button`/`.danger-button`/`.danger-text-button`/`.customer-booking-link`/`.history-undo` rules (base + every scoped/media-query variant) from `styles.css` — adb2b, 2026-08-15
- Wired the already-scaffolded `AppPagination.vue` (Tailwind-based, prop-driven, supports both compact "Seite X von Y" and full page-number-list modes via slots for custom prev/next icons) into its three real call sites — `CustomersPage.vue` (page-number list), `BookingsPage.vue` and `CheckInOutPage.vue`'s history list (compact mode) — replacing their hand-rolled `<nav class="pagination">` markup, verified live via Playwright screenshots (page-2 navigation on customers and check-in/out history); removed the now-dead `.pagination`/`.pagination-ellipsis`/`.history-pagination` rules (base + mobile `flex-wrap` override) from `styles.css` — f2484, 2026-08-15 20:05
- Rebuilt the shared `BaseModal.vue` chrome (backdrop, box, close button, per-modal width) with Tailwind utility classes, incl. `sm:` variants replacing the old `@media(max-width:680px)` overrides for modal padding and the action-button row (which now stacks full-width/reversed on mobile via `flex-col-reverse … sm:flex-row [&>*]:w-full sm:[&>*]:w-auto` instead of a media query), and inlined the shared icon/heading/description/`dl`/actions styling directly in all 13 modal components (`CancelAccountModal`, `CheckInModal`, `CheckoutModal`, `DeleteBookingModal`, `DeclineRequestModal`, `CustomerFormModal`, `ClosureFormModal`, `RoomFormModal`, `RoomChangeModal`, `BookingFormModal`, `OccupancyReservationModal`, `RequestAssignModal`, `PetFormModal`); removed the now-dead `.modal-backdrop`/`.modal`/`.modal-close`/`.modal-icon`/`.modal h2`/`.modal>p`/`.pet-note`/`.modal dl/dt/dd`/`.modal-actions`/`.cancel-icon`/`.checkout-icon`/all `*-modal{width:…}` rules from `styles.css` (kept the semantic `*-form-modal` classnames on elements since `dashboard-search.spec.ts` asserts `.booking-form-modal` CSS); verified via the full modal-related Playwright suite (desktop + mobile) and live screenshots — 51144, 2026-08-15 21:15
- Introduced `AppEmptyState.vue` (Tailwind-based, `icon`/`title`/`description` named slots plus a plain default slot, with an `inline` variant for the left-aligned/tight-padding case) and replaced all 9 raw `.empty-state`/`.empty-state.compact` usages across `App.vue`, `BookingsPage.vue`, `CheckInOutPage.vue`, `CustomersPage.vue` (×2), `OccupancyPage.vue`, `SettingsPage.vue`, and `RequestsPage.vue` (×3) with it; removed the now-dead `.empty-state`/`.occupancy-closures>.empty-state` rules from `styles.css` (the `.compact` modifier had no CSS rule and was dropped as dead markup); verified via `vue-tsc --noEmit` and a live Playwright screenshot of the customer-search no-match state plus the occupancy closures inline state — 46692, 2026-08-15
- Rebuilt `DemoDataControl.vue` (the "Demodaten" badge/dropdown in the topbar) with Tailwind utility classes, using a `sm:` variant instead of the old `@media(max-width:680px)` override to switch between the full pill badge (icon+text+inline dot) and the collapsed circular badge (icon only, dot absolutely positioned top-right) — also dropped the dead `!important` width/typography overrides at the bottom of `styles.css` that were overriding the base `.demo-menu`/`.demo-menu-info` rules, since the menu width is now just a single `w-[264px]` utility; removed the now-dead `.demo-control`/`.demo-badge`/`.demo-menu`/`.demo-menu-icon`/`.demo-menu header/strong/small/p/dl/dt/dd` rules (base + mobile media query + the later `!important` overrides) from `styles.css`; verified via `vue-tsc --noEmit` and live Playwright screenshots of the closed/open states at both desktop (1280px) and mobile (390px) — fda38, 2026-08-15 20:43
- Cleaned up `CustomersPage.vue`'s master-detail layout (`.customer-layout` two-column grid, mobile back-button toggle, and the tangled/duplicated `.customer-profile-header` rules — incl. an exact-duplicate `@media(max-width:1050px)` block and conflicting `!important` overrides across three separate media queries) by moving it to Tailwind utility classes (`min-[1050px]:grid-cols-[...]`, `hidden`/`block` state toggling, `sm:flex-row`) directly in the template; removed the now-dead `.customer-layout`/duplicate `@media(max-width:1050px)` block/`.customer-profile-header`/`.customer-profile-intro`/`.customer-contact-links`/`.customer-profile-actions` rules (base + all three conflicting media-query variants) from `styles.css`; verified pixel-identical behavior at 1280px/1000px/390px via live Playwright screenshots (list view, details view, back-navigation) and `vue-tsc --noEmit` — 3706e, 2026-08-15 20:38
- Introduced `AppIconButton.vue` (Tailwind-based, `default`/`danger` variant, forwards a `focus()` method via `defineExpose` so existing `ref`-based focus-management call sites keep working) and replaced all 11 raw `.icon-button`/`.icon-button.danger-icon-button` usages across `App.vue` (mobile nav open/close, incl. moving the `.menu-button`/`.close-nav` mobile-only display toggle and close-button's absolute positioning from a `@media(max-width:920px)` rule to `hidden max-[920px]:grid` utility classes), `BaseModal.vue`, `DemoDataControl.vue`, `OccupancyPage.vue` (closure edit/delete), `SettingsPage.vue` (room edit/delete, incl. the bordered red `danger` variant), `BookingsPage.vue` (calendar week prev/next), and `ToastRegion.vue` with it; removed the now-dead `.icon-button`/`.icon-button:hover`/`.menu-button,.close-nav{display:none}`/the mobile-media-query `.menu-button,.close-nav{display:grid}.close-nav{position:absolute;...}`/`.icon-button.danger-icon-button` (incl. `:disabled`) rules from `styles.css`; verified via `vue-tsc --noEmit` and live Playwright screenshots (settings rooms list incl. disabled danger delete, occupancy closures, bookings calendar week nav, mobile sidebar open/close, room-create modal close) — a15b6, 2026-08-15
- Introduced `AppTabs.vue`/`AppTab.vue` (Tailwind-based pill tab-switcher, `AppTab` renders `<button>` or `RouterLink` via `to` prop like `AppButton`, with `size`/`grow-mobile` props) and replaced all 4 hand-rolled pill-switcher instances — `.schedule-tabs` (dashboard Anreisen/Abreisen in `App.vue`), `.booking-view-switch` (`BookingsPage.vue` Liste/Zeitachse), `.range-filters` (`OccupancyPage.vue` 7/14/30 Tage, incl. its mobile full-width `flex:1` override), and `.settings-tabs` (`SettingsPage.vue` RouterLink tabs, which also dropped the accidental `nav a.router-link-exact-active::before` bleed-through from the sidebar's global `nav a` selector by no longer rendering a bare `<nav><a>`) — removed the now-dead `.schedule-tabs`/`.booking-view-switch`/`.range-filters`/`.settings-tabs` rules (base + all mobile media-query variants) from `styles.css`; verified via `vue-tsc --noEmit` and live Playwright screenshots of all 4 call sites at desktop (1280px) and mobile (390px), incl. toggling each tab's active state — c71cd, 2026-08-15 20:49
- Rebuilt `CheckInOutPage.vue`'s page-specific chrome (date-nav, the 3-up view-switch summary cards, the operations-list header/search/rows, and the check-in-out history rows) with Tailwind utility classes (incl. its grid system, exact-pixel `minmax(...)` grid tracks preserved via arbitrary values, and `max-[Npx]:`/`max-sm:` variants replacing all `@media(max-width:680/760px)` overrides), moving the history-event-icon color variants into a `historyEventIconClasses` map like `AppMetricCard`'s pattern; along the way found and fixed a real bug this migration would otherwise have hit — `.panel>header{display:flex}`'s child-combinator selector (specificity 0,1,1) silently beats a plain Tailwind `.grid` utility class (0,1,0) regardless of source order, which is *why* the original CSS had a `display:grid!important` on this header — replicated that with Tailwind's `!grid` important-prefix; removed the now-fully-dead `.operations-date-nav`/`.operations-date-input`/`.operations-today`/`.operations-date-label`/`.operations-summary`/`.operations-list-header`/`.operations-search`/`.operation-row`/`.operation-room`/`.operation-time`/`.operation-actions`/`.operations-history`/`.history-rows`/`.history-event-icon`/`.history-event-time` rules (base + every 680/760px media-query variant, and two rules that were already dead/non-matching before this change) from `styles.css`, keeping the shared `.list-header-actions`/`.pet-avatar`/`.pet-info`/`.panel` rules untouched since other pages still use them; verified via `vue-tsc --noEmit`, the full `vitest` suite (212 passed), and pixel-diffed Playwright screenshots (desktop 1280px + mobile 390px, checked-in and arrivals views) against the pre-change render — eebf5, 2026-08-16
- Rebuilt the dashboard's `App.vue` arrivals/departures panel and occupancy panel (the two-column `.dashboard-grid`, `.arrival-row`/`.departure-row` mixed-column article grids incl. the `.arrival-note-slot` "Hinweis" badge slot, `.panel-navigation`, `.panel-title-link`, `.occupancy-number`, the `.progress` bar, and `.room-summary` rows) with Tailwind utility classes, replicating the original grid-template-columns/explicit-placement selectors 1:1 via `max-[680px]:`/`min-[921px]:` arbitrary variants so the browser's own auto-placement algorithm reproduces the exact prior layout instead of me having to hand-derive it; removed the now-dead `.dashboard-grid`/`.arrival-row`/`.departure-row`/`.arrival-meta`/`.arrival-note-slot`/`.panel-navigation`/`.occupancy-number`/`.progress`/`.room-summary`/`.panel-title-link`/`.arrivals-panel`/`.occupancy-panel` rules (base + the 920px/680px media-query variants) from `styles.css`, keeping the shared `.panel`/`.pet-avatar`/`.pet-info`/`.note-badge`/`.room-icon` rules untouched since other pages still use them; verified via `vue-tsc --noEmit`, the full `vitest` suite (212 passed), and pixel-diffed Playwright screenshots (desktop 1280px, tablet 900px, mobile 390px — arrivals view incl. the Hinweis badge row, and departures view) against the pre-change render — 5dd47, 2026-08-16
- Rebuilt the shared "settings form" chrome (`.settings-layout`, `.settings-panel>header`, `.settings-icon` incl. `teal`/`amber`/account's `danger-icon` variants, `.settings-fields`/`.time-fields`, `.currency-input`, `.settings-actions`) used by `SettingsPage.vue` (Allgemein/Tarife tabs) and `AccountSettingsPage.vue`, plus the room-management section (`.room-settings-panel`, `.room-list`, `.room-list-actions`, `.room-form-error`) and `RoomFormModal.vue`'s `.room-form-modal-form`, and `AccountSettingsPage.vue`'s `.cancellation-notice`/`.danger-zone-body`/`.account-role-badge` — all with Tailwind utility classes (incl. `max-[680px]:` variants replacing every `@media(max-width:680px)` override, and `!justify-start`/`!border-[...]` important-prefixed utilities where needed to beat the higher-specificity shared `.panel`/`.panel>header` selectors); removed the now-fully-dead `.settings-layout`/`.settings-panel`/`.settings-icon`/`.settings-fields`/`.currency-input`/`.settings-actions`/`.room-settings-panel`/`.room-list`/`.room-list-actions`/`.room-form-error`/`.room-form-modal-form`/`.account-page .settings-layout`/`.danger-zone`/`.danger-icon`/`.danger-zone-body`/`.cancellation-notice`/`.account-role-badge` rules (base + all 680px media-query variants + the tail font-size overrides) from `styles.css`; verified via `vue-tsc --noEmit`, the full `vitest` suite (212 passed), and live Playwright screenshots of all three settings tabs plus the account page (incl. the cancelled-account banner) at desktop (1280px) and mobile (390px) — session 42307, 2026-08-16 13:32
- Rebuilt `RequestsPage.vue`'s open-requests cards (`.request-cards`/`.request-card`/`.request-summary`/`.request-species-icon`/`.request-info`/`.request-dates`/`.request-availability`/`.request-actions`/`.request-buttons`, incl. the `page-count` header badge) and its `.request-history` rows with Tailwind utility classes, replacing the named-grid-area layout with explicit `row-start`/`col-start`/`row-span` placement plus `min-[900px]:`/`max-[900px]:` variants instead of `grid-template-areas` and the old `@media(max-width:900px)` block, and inlined `DeclineRequestModal.vue`'s `.decline-request-form`/`.modal-notification-hint` and `RequestAssignModal.vue`'s `.request-assign-form`/`.customer-match-hint`/`.room-occupancy-hint` the same way (kept the bare `request-card`/`request-summary`/`request-actions`/`request-history`/`request-availability` classnames on their elements since `e2e/dashboard-search.spec.ts` selects on them); also dropped the already-dead `.decline-reason-field`/`.decline-reason-input` rules (no matching markup anywhere); removed the now-fully-dead `.page-count`/`.requests-list`/`.request-cards`/`.request-card`/`.request-summary`/`.request-species-icon`/`.request-info`/`.request-dates`/`.request-availability`/`.request-actions`/`.customer-match-hint`/`.room-occupancy-hint`/`.request-buttons`/`.decline-request-form`/`.request-assign-form`/`.modal-notification-hint`/`.request-notification`/`.request-history` rules (base + the 900px media query + the tail font-size overrides) from `styles.css`, keeping the shared `.booking-status{align-self:center;justify-self:end}` rule since `CustomerDetail`'s un-migrated booking-history grid still relies on it; verified via `vue-tsc --noEmit`, the full `vitest` suite (212 passed), and live Playwright screenshots of the open-requests cards, both the assign and decline modals, and the Verlauf history rows at desktop (1280px) and mobile (390px) — session 2b893, 2026-08-16
- Rebuilt `CustomersPage.vue`'s customer-detail sections (`.detail-section`/`.section-title`, the pet profile cards incl. `.pet-profile-grid`/`.pet-profile-card`/`.pet-profile-avatar`/`.pet-profile-summary`/`.pet-card-actions`/`.pet-edit-button`/`.pet-remove-button`/`.pet-removal-confirmation`, and the `.booking-history` rows) plus the customer-removal `.emergency-contact-removal` alert with Tailwind utility classes, switching the pet card from the old two-column CSS-grid-with-implicit-row-wrapping layout to a plain flex layout; along the way found and fixed a real bug from an earlier session's layout migration (item 10) — that migration dropped the `.pet-profile-grid` wrapper div from the template but left `.pet-profile-grid strong`/`.pet-profile-grid .pet-special-food-badge`/`.pet-profile-grid p` as descendant-selector CSS keyed off that now-absent ancestor class, so the "Besonderes Futter" badge and the pet-note box had been rendering as unstyled plain text/inline text in production ever since — now styled directly and verified live; kept the bare `pet-profile-card`/`pet-profile-avatar` classnames on their elements since `e2e/dashboard-search.spec.ts` selects on them (that particular test was already failing before this change for an unrelated reason — the pet data model has no `breed`/species-label fields the test expects — pre-existing gap, left untouched); also dropped the fully-dead `.emergency-contact-actions`/`.pet-medication-plan`/`.veterinary-contact-row` rules (no matching markup anywhere) from `styles.css`, keeping the shared `.booking-status` base+color rules since `BookingsPage.vue`/`RequestsPage.vue` still use them; verified via `vue-tsc --noEmit`, the full `vitest` suite (212 passed), and live Playwright screenshots (single-pet with note+badge, two-pet with a removal-confirmation open, desktop 1280px + mobile 390px) — a6399, 2026-08-16 13:41
- Rebuilt the customer-directory search box and result list (`.directory-search`, `.customer-list`, `.customer-avatar`, `.pet-count`, `.customer-details-back`) shared between `CustomersPage.vue`'s master list and `BookingsPage.vue`'s list-header search field with Tailwind utility classes, incl. the active-row inset accent border and the `.bookings-list>header .directory-search{width:min(340px,45%)}`/mobile-full-width overrides now expressed via a plain `sm:w-[min(340px,45%)]` variant; dropped the now-dead `.customer-avatar.large` modifier (unused in the codebase) and the tail font-size overrides (`.customer-list strong/small`, `.customer-avatar`) by inlining them directly; removed the now-fully-dead `.directory-search`/`.customer-list`/`.customer-avatar`/`.pet-count`/`.customer-details-back` rules (base + both `.bookings-list>header .directory-search` variants + the combined `.directory-search.disabled` selector, keeping `.booking-filters.disabled` intact) from `styles.css`; verified via `vue-tsc --noEmit`, the full `vitest` suite (212 passed), and a live Playwright screenshot of the customer directory (search box, avatars, active-row highlight, pet-count badges) — b8116, 2026-08-16
- `CustomerAutocomplete.vue` (Eingabefeld, Vorschlags-Dropdown und aktive Auswahl) auf Tailwind-Utilities umgestellt und die zugehörigen globalen CSS-Regeln entfernt — bcde8, 2026-08-16 12:42
- Buchungs-Filterleiste (Statuschips, Datumsfilter und Auswahlhinweis) in `BookingsPage.vue` auf responsive Tailwind-Utilities umgestellt und die zugehörigen globalen CSS-/Media-Query-Regeln entfernt — 1eb97, 2026-08-16 12:43
- Buchungszeitachse in `BookingsPage.vue` auf Tailwind-Grid und responsive Utilities umgestellt (inkl. Wochenraster, Aufenthaltsbahnen sowie Navigation mit `AppButton`/`AppIconButton`); Build und 212 Tests erfolgreich — 413b4, 2026-08-16 13:37
- Check-out-Preisberechnung in `CheckoutModal.vue` auf lokale Tailwind-Grid-Utilities umgestellt und die globalen `.checkout-price`-/`.checkout-price-unavailable`-Regeln entfernt — ff151, 2026-08-16 13:37
- Tierauswahl der Buchungs- und Auslastungsreservierungsdialoge als wiederverwendbare Tailwind-Komponente `AppPetSelection` mit `v-model` und Change-Event umgesetzt; doppelte Markup-Blöcke sowie globale `.pet-selection`-/`.pet-choice`-Regeln mit `!important` entfernt — 5a47b, 2026-08-16 13:39
- Wiederkehrende Checkbox-Felder als Tailwind-Komponente `AppCheckboxField` in Tier- und Einstellungen-Formularen gebündelt und die globale `.checkbox-field`-Regel mit `!important` entfernt — bf15e, 2026-08-16 13:40
- Schließzeiten-Liste der Belegungsansicht auf lokale Tailwind-Utilities umgestellt und die globalen `.occupancy-closures`-/`.closure-list`-/`.closure-actions`- sowie toten Dialogformular-Regeln entfernt; Build erfolgreich — 68dd5, 2026-08-16 13:41
- Wiederkehrende Kundenprofil-Links als Tailwind-Komponente `AppCustomerLink` gebündelt, in Buchungen, Check-in/out, Anfragen und den Bestätigungsdialogen verwendet und die globalen `.customer-profile-link`-Regeln entfernt; Build erfolgreich — bfe8d, 2026-08-16 13:42
- Reservierungsformulare in `BookingFormModal.vue` und `OccupancyReservationModal.vue` auf Tailwind-Grid/-Utilities umgestellt, `AppFormField.vue` als wiederverwendbare Feldhülle ergänzt und die zugehörigen globalen Formular-, Preisvorschau- sowie Mobile-Regeln entfernt — 89f3b, 2026-08-16 13:43
- Buchungsstatus-Chips als wiederverwendbare Tailwind-Komponente `AppBookingStatus` gebündelt, in Buchungs-, Kunden- und Anfrageverlauf eingesetzt und die globalen `.booking-status`- einschließlich mobiler Grid-Regeln entfernt; Build erfolgreich — Session 99762, 2026-08-16 13:44
- Buchungslisten-Zeilen in `BookingsPage.vue` (inkl. Grid, mobiler Anordnung, Fokuszustand, Notiz und Aktionen) auf lokale Tailwind-Utilities umgestellt und die globalen `.booking-table`-/Aktions-/Media-Query-Regeln entfernt — Session 57bff, 2026-08-16 13:46
- Überbuchungswarnungen als wiederverwendbare Tailwind-Komponente `AppOverbookingWarning` gebündelt, in vier Dialogen eingesetzt und die globale `!important`-Regel entfernt; Build erfolgreich — Session d87d9, 2026-08-16 14:33
- Fallback-/404-Ansicht in `App.vue` auf lokale Tailwind-Utilities umgestellt und die globalen `.route-page`-/`.route-page-card`-/`.route-page-icon`-Regeln entfernt — Session 71e79, 2026-08-16 14:34

- Desktop-Datumssteuerung des Belegungsrasters in `OccupancyPage.vue` auf lokale Tailwind-Utilities umgestellt (inklusive Auslastungs-, Heute-, Hover- und Fokuszuständen) und die zugehörigen globalen `.occupancy-day-header`-Regeln entfernt; Produktionsbuild erfolgreich — Session 5a7bb, 2026-08-16 15:02
- Belegungslegende als Tailwind-basierte `AppOccupancyLegend`-Komponente gekapselt und die globalen Legenden- sowie Header-Media-Query-Regeln entfernt; Build erfolgreich — Session ea430, 2026-08-16 14:39
- Consolidated recurring form error messages in the Tailwind-based `AppFormError` component, used it in five form views, and removed the global `.form-error` rule; build successful — Session 56eec, 2026-08-16 14:38
- Wiederkehrende Eyebrow-Überschriften als Tailwind-Komponente `AppEyebrow` gebündelt, in Seiten- und Modalüberschriften verwendet und die globalen `.eyebrow`-Regeln entfernt; Build und Typprüfung erfolgreich — Session 18de5, 2026-08-16 14:36
- Occupancy date controls migrated to Tailwind layout utilities and reusable AppButton/AppIconButton components; removed the remaining global controls, date-navigation, and mobile media-query CSS rules; build successful — Session c7484, 2026-08-16 14:37
- Den gesperrten Zimmerstatus der Belegungsansicht als Tailwind-basierte `AppStatusBadge`-Komponente gekapselt und die globale `.status-badge`-CSS-Regel entfernt; Build erfolgreich — Session 6ac4d, 2026-08-16 14:38
- Wiederkehrende Tier-Avatare und Hinweis-Badges als Tailwind-Komponenten `AppPetAvatar` und `AppNoteBadge` gebündelt, in Dashboard, Buchungen, Check-in/out und Anfragen eingesetzt sowie die globalen `.pet-avatar`/`.pet-info`/`.note-badge`-Regeln entfernt; Build erfolgreich — Session 4e3d6, 2026-08-16 14:41
- Zimmerkategorie-Icons als Tailwind-basierte `AppRoomIcon`-Komponente gebündelt, im Dashboard und in der Zimmerverwaltung eingesetzt und die globale `.room-icon`-Regel entfernt; Produktionsbuild erfolgreich — Session 8b229, 2026-08-16 14:42
- Wiederkehrende Header-Aktionsleisten in Buchungen, Check-in/out, Kunden und Belegung durch lokale Tailwind-Flex-Utilities ersetzt und die globale `.list-header-actions`- inklusive Mobile-Media-Query-Regeln entfernt; Produktionsbuild erfolgreich — Session ef996, 2026-08-16 15:01
- Klickbarer Datumstitel der mobilen Belegungsansicht in `OccupancyPage.vue` auf lokale Tailwind-Utilities umgestellt und die globale `.mobile-occupancy-date`-Regel entfernt; Produktionsbuild erfolgreich - Session 6ee4e, 2026-08-16 15:02
- Topbar und Account-Avatar in `App.vue` auf lokale Tailwind-Utilities umgestellt und die globalen `.topbar`/`.top-avatar`-Regeln einschließlich des mobilen Media-Query-Overrides entfernt; Produktionsbuild und Typprüfung erfolgreich — Session 24328, 2026-08-16 15:09
- Die klickbare Tierzeile in der Buchungszeitachse lokal mit Tailwind-Utilities gestylt und die verbleibende globale `.booking-timeline-lane article button`-CSS-Regel entfernt — Session 91bf4, 2026-08-16 15:03
- Die Check-in/out-Kennzahl-Icons als Tailwind-basierte `AppMetricIcon`-Komponente gekapselt, die drei Varianten eingebunden und die globalen `.metric-icon`-/Farbregeln entfernt; Produktionsbuild erfolgreich - Session 762e0, 2026-08-16 15:04
- Die responsive Aufenthaltsliste im Kundenprofil auf lokale Tailwind-Grid-Utilities umgestellt und die verbleibende globale Mobile-Media-Query sowie tote `.pet-card`-Schriftregeln entfernt; Produktionsbuild erfolgreich - Session d4fed, 2026-08-16 15:08
- App-Chrome mit Seitenleiste, Navigation, mobilem Scrim und Hauptbereich in `App.vue` auf lokale Tailwind-Utilities umgestellt und die zugehörigen globalen CSS-/Media-Query-Regeln entfernt; Produktionsbuild und Typprüfung erfolgreich — Session a7ee3, 2026-08-16 15:11
- Mobile Tageslisten-Ansicht der Belegung in `OccupancyPage.vue` auf lokale Tailwind-Grid-/Utility-Klassen umgestellt und den zugehörigen globalen CSS- sowie Media-Query-Block entfernt; Produktionsbuild erfolgreich — Session ed8ff, 2026-08-16 15:06
- Desktop-Belegungsraster in `OccupancyPage.vue` auf lokale Tailwind-Grid-/Utility-Klassen umgestellt und die globalen Raster-, Status- und Zellenregeln entfernt; Produktionsbuild erfolgreich — Session b3161, 2026-08-16 15:07
- Gemeinsame Panel-Chrome als Tailwind-basierte `AppPanel`-Komponente gekapselt und alle 18 verbliebenen `.panel`-Wurzeln in Dashboard, Buchungen, Check-in/out, Kunden, Belegung, Anfragen und Einstellungen migriert; die letzte globale `.panel`-Regel samt Media Query aus `styles.css` entfernt; Produktionsbuild erfolgreich — Session 6ae38, 2026-08-16 15:20
- Die verbliebenen Scoped-CSS-Slotregeln von `AppPanel` vollständig durch Tailwind-Utilities am Komponenten-Root ersetzt; Produktionsbuild erfolgreich — Session e9896, 2026-08-16 15:21
