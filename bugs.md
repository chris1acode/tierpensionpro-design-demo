[x] Umbrechen der Dashboard zahlen auf mittleren Screen sind komisch aus. Das Icon sollte noch oben rutschen.
[x] Anzahl Kunden Badge ist unnötig, kann weg
[x] Belegung planen muss heißen Belegung ansehen. Und soll nur link sein auf Titel. Der Button kann weg.
[x] Belegung (Mobile, ≤680px): Die Kopfzeile "Zeitraum" (7/14/30 Tage) und der Button "Reservierung anlegen" standen im selben nicht umbrechenden Flex-Container wie die Datumsnavigation. Der Button bekam zwar `width:100%`, blieb aber Flex-Item ohne Zeilenumbruch und ragte dadurch rechts über den 390px-Viewport hinaus; sein unsichtbarer Klickbereich überlagerte andere Buttons ("30 Tage" fing z. B. Klicks auf "Reservierung anlegen"/"Schließzeit anlegen" ab). Gefunden über eine per Playwright fehlschlagende Testreihe auf der mobilen Belegungsseite (4 Tests mit "element intercepts pointer events" auf `.range-filters`). Fix in `src/styles.css`: `.occupancy-controls{flex-wrap:wrap}` in der 680px-Media-Query ergänzt, `.range-filters` erhält `width:100%` und die einzelnen Buttons `flex:1`, sodass Datumsnavigation, Zeitraum-Auswahl und Reservierungs-Button auf schmalen Screens sauber untereinander stehen statt sich zu überlappen.
[x] Der standard bei Checkin soll "Jetzt eingecheckt sein" es sei denn man navigiert von aussen explizit auf eine Unterseite. `src/components/CheckInOutPage.vue`: `activeView` ist bereits standardmäßig `'checked-in'` und wird nur über `?view=arrivals`/`?view=departures` explizit auf eine Unterseite umgestellt (`applyView`) – entspricht der Vorgabe. Der bisher widersprüchliche Seitenuntertitel ("Alle anstehenden Anreisen und Abreisen zentral bearbeiten") wurde angepasst auf "Eingecheckte Tiere sowie anstehende Anreisen und Abreisen zentral bearbeiten.", damit er zum Default-Tab passt.
[ ] Beim Zimmerwechsel muss Überbuchung möglich sein.