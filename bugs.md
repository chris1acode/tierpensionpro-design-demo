[x] There is no router implemented
[x] 404 page is missing
[x] A missing space between Tierpension and Pro
[x] Wenn man sucht und ein Treffer nicht vorhanden ist, steht da "Alle erwarteten Tiere sind angekommen." statt "Keine Treffer gefunden." — behoben in SessionId 86236
[x] Pflegeaufgaben werden nicht benötigt, kann weg — entfernt in SessionId 9f9dc
[x] Zahlungen werden nicht benötigt, kann weg — entfernt in SessionId c5efc
[x] Gäste im Haus muss Tiere im Haus sein — behoben in SessionId d47d5
[x] So viele Mock Daten erzeugen, dass auch Paging funktioniert — umgesetzt in SessionId d34d7
[x] Hauptstandort wird nicht benötigt, kann weg — behoben in SessionId f3cf0
[x] Kundenverzeichnis ist zu gequetscht, sollte 2/3 des Contents einnehmen — behoben in SessionId a2e84
[x] Kundenverzeichnis mit Details funktioniert nicht gut in kleiner Dimension. Untereinander macht keinen Sinn. Das muss dann anders gelöst werden. — durch responsive Master-Detail-Navigation behoben in SessionId d3a9b
[x] Playwright-Test für die Desktop-Breite des Kundenverzeichnisses verwendete den veralteten Pfad `/kunden` und lief in einen Timeout — auf `/kunden-tiere` korrigiert in SessionId d3a9b
[x] Reinigung wird nicht benötigt, Funktion weg! — entfernt in SessionId b98cc
[x] Der im globalen Suchfeld beworbene Tastaturkurzbefehl Cmd/Ctrl+K fokussiert die Suche nicht — behoben in SessionId af239
[x] Modale Dialoge übernehmen beim Öffnen nicht den Tastaturfokus und lassen sich nicht mit Escape schließen — behoben in SessionId 25810
[x] Fehlender Viewport-Meta-Tag verhindert auf echten mobilen Browsern die vorgesehenen Responsive-Breakpoints; die geschlossene Seitennavigation bleibt dadurch sichtbar beziehungsweise fokussierbar — behoben in SessionId 11ba2
[x] Der Tastaturfokus kann mit Tab beziehungsweise Shift+Tab aus einem als modal ausgezeichneten Dialog in die Seite dahinter wechseln — behoben in SessionId 6f631
[x] Buchungen müssen in Kombination mit Kunde geschehen. — kundengeführte Buchungsanlage mit validierter Kunden-Tier-Zuordnung umgesetzt in SessionId b17f4
[x] Beim Öffnen der mobilen Navigation bleibt der Tastaturfokus auf der Seite dahinter und der Drawer lässt sich nicht mit Escape schließen — Fokusübergabe, Escape-Schließen und Fokuswiederherstellung umgesetzt in SessionId c04dc
[x] Der Tastaturfokus kann mit Tab beziehungsweise Shift+Tab aus der geöffneten mobilen Navigation in die verdeckte Seite wechseln — modale Semantik und zyklische Fokusführung umgesetzt in SessionId 24b80
[x] Die globale Dashboard-Suche filtert nur Anreisen; im Tab „Abreisen“ bleiben trotz Suchbegriff alle Einträge sichtbar — einheitliche Filterung und Such-Leerzustand umgesetzt in SessionId 5a42f
[x] Cmd/Ctrl+K verschiebt den Tastaturfokus trotz geöffnetem Dialog beziehungsweise mobiler Navigation in das Suchfeld der verdeckten Seite — globalen Suchshortcut während modaler Oberflächen gesperrt in SessionId 3e9fe
[x] Hauptmenupunkt Anfragen fehlt — per Einstellung schaltbares Anfragen-Postfach mit echtem Anfrage-Datenmodell, Annahme- (inkl. automatischer Kunden-/Tier-Zuordnung als Reservierung) und Ablehnen-Workflow unter /anfragen umgesetzt in SessionId bac0c
[x] Hauptmenupunkt Belegung fehlt — Tages-Spaltenansicht mit Auslastungsanzeige unter /belegung umgesetzt in SessionId 47162
[x] Vier Tests in `usePensionStore.test.ts` (u. a. Check-in, Kapazitätsprüfung, Demo-Reset, isolierte Store-Instanzen) verwenden `createPensionStore()` ohne injiziertes `now`; die Mock-Buchung `b-1` ist fest auf den Anreisetag 2026-08-09 datiert, wodurch diese Tests fehlschlagen, sobald die reale Systemzeit dieses Datum überschreitet — beim Prüfen der Testsuite in SessionId 47162 festgestellt und in SessionId bac0c behoben, indem diesen Tests dasselbe fest injizierte Datum (2026-08-09) übergeben wurde wie den bereits vorhandenen datumsabhängigen Tests.
[x] Die eigentliche Ursache des vorigen Punkts war in SessionId bac0c nur in den Unit-Tests kaschiert, nicht in der Anwendung selbst behoben: `usePensionStore.ts` verwendet für die echte App (`App.vue`/`main.ts`) weiterhin `defaultDependencies.now = () => new Date()`, also die reale Systemzeit, während sämtliche Mockdaten (Anreisen/Abreisen von April bis August 2026) fest auf den Demo-Tag 2026-08-09 verankert sind. Dadurch zeigte das Dashboard am 2026-08-10 in Playwright-Tests (`e2e/dashboard-search.spec.ts`) plötzlich die falsche Anzahl offener Vorgänge und die erwartete Anreise „Balu“ fehlte — die Demo bricht so an jedem Tag, der nicht der 2026-08-09 ist. Behoben in SessionId 0a8f0, indem `defaultDependencies.now` auf das feste Demo-Datum (2026-08-09, 12:00) statt auf `new Date()` gesetzt wurde.
[x] Bei den Anfragen muss der Benutzer groß sein und das Tier klein. — Kund:innenname jetzt hervorgehoben, Tierinformationen sekundär dargestellt, behoben in SessionId 70796
[x] Bei den Anfragen fehlt eine bessere Ansicht der Auslastung. Die könnte man zum Beispiel einblenden, wenn man das Zimmer auswählt — Auslastungshinweis (Spitzenbelegung im Anfragezeitraum, engster Tag, Ausbuchungswarnung) blendet sich nach Zimmerauswahl ein, behoben in SessionId 70796
[x] Der Kundenname steht immer im Vordergrund. Er muss groß sein und das Tier klein. Falsch in Anfragen und Checkin — Verlauf in Anfragen sowie Anreisen-/Abreisenliste auf dem Dashboard und die Check-out-Modal-Überschrift korrigiert, behoben in SessionId 4bf57
[x] Die Checks beim Abreisen sind obsolet. Weg damit — Übergabe-Checkliste (persönliche Sachen, Medikamente, Zimmerkontrolle) vollständig aus Datenmodell, Store und Check-out-Dialog entfernt; Check-out ist jetzt eine einfache Bestätigung wie der Check-in, behoben in SessionId 4bf57
[x] Die E-Mail-Validierung für Kontodaten (`domain/account.ts`) und Pensions-Einstellungen (`domain/pensionSettings.ts`) prüfte nur, ob die Eingabe irgendwo ein „@“ enthält (`email.includes('@')`). Werte wie „@“, „a@“, „@b.com“ oder „justtext@“ wurden dadurch als gültige E-Mail-Adresse akzeptiert und gespeichert, sobald die native HTML5-`type="email"`-Prüfung umgangen wurde (z. B. bei abweichendem Eingabeweg). Da die Kontodaten laut eigener Beschriftung „für Anmeldung und Benachrichtigungen“ verwendet werden, hätte eine offensichtlich unbrauchbare Adresse unbemerkt persistiert werden können — behoben in SessionId cf3c6 durch eine gemeinsame, echte Format-Policy `domain/email.ts` (`isValidEmail`), die in beiden Validierungen verwendet wird.
[x] Beim Öffnen des globalen Demodaten-Menüs blieb der Tastaturfokus auf der auslösenden Schaltfläche hinter dem eingeblendeten Panel; Escape schloss das Menü, ohne den Fokus nachvollziehbar zurückzuführen — Fokusübergabe an die Schließen-Schaltfläche und Fokuswiederherstellung nach Escape umgesetzt in SessionId fd71e.
[x] In der Ansicht „Anfragen“ wurde die unbelegte Zimmerauswahl leer statt mit der vorgesehenen Vorauswahl „Zimmer wählen“ dargestellt. Damit war der nötige nächste Schritt zum Annehmen einer Anfrage nicht erkennbar — behoben in SessionId bbc90 durch einen expliziten leeren Modellwert und einen Playwright-Regressionstest.
[x] In der Liste von Heutige Anreisen sind die Elemente nicht vertikal aligned — die Check-in-Aktion belegt nun auch ohne optionalen Hinweis stets dieselbe Grid-Spalte; mit einem Desktop-Playwright-Regressionstest abgesichert, behoben in SessionId 4902d
[x] Im Konto-Kündigungsdialog konnte Cmd/Ctrl+K den Fokus in die globale Suche hinter dem geöffneten Modal verschieben. Die Shortcut-Sperre kannte nur die vom Dashboard selbst verwalteten Dialoge, nicht jedoch den eigenständigen `CancelAccountModal` — behoben in SessionId 4e1ef durch eine allgemeine Prüfung auf ein geöffnetes `aria-modal`-Dialogelement; per Playwright auf Desktop und Mobile abgesichert.
[x] Analysiere bug-screenshots/screen1.png und fixe alignment — die optionale Hinweis-Markierung erzeugte je nach Buchung eine unterschiedliche Grid-Belegung; ein immer vorhandener Hinweis-Slot hält Zeit/Zimmer-Metadaten und Check-in-Aktion zeilenübergreifend ausgerichtet, behoben in SessionId bb23b
[x] Nach dem Zurücksetzen der Demodaten behielten die bereits geöffneten Formulare für Pensionseinstellungen und Konto ihren alten Entwurf. Dadurch zeigte die Oberfläche nicht den wiederhergestellten Stand und ein anschließendes Speichern konnte die zurückgesetzten Werte erneut überschreiben — behoben in SessionId 46c45 durch eine Store-Synchronisierung der Entwürfe; per Playwright abgesichert.
[x] Die Belegungsauslastung pro tag ist mist. Viel zu große cards. Das muss weg. Stattdessen soll die belegung minimalistisch als Füllstand im Kartenblatt angezeigt werden. Und in der Tabelle keine konkreten namen, nur anzahlen zeigen. — kompakte Füllstände direkt in den Tages- und Zimmerzellen, ohne separate Tageskarten oder Tier-/Kundennamen, umgesetzt in SessionId 3d5d3
[x] Die Zimmerauswahl in Buchungen und Anfragen führte alle betriebsbereiten, tierartkompatiblen Zimmer auf, auch wenn diese im gesamten gewählten Zeitraum bereits voll belegt waren. Das Formular ließ die Auswahl zu und meldete erst beim Speichern einen allgemeinen Fehler. — behoben in SessionId e4ab5 durch kapazitätsbasierte Filterung mit der vorhandenen `hasRoomCapacityForStay`-Policy; mit einem Playwright-Regressionstest auf Desktop und Mobile abgesichert.
[x] Die fest am rechten unteren Bildschirmrand platzierte Demodaten-Schaltfläche überdeckte auf dem Desktop-Dashboard die primäre Aktion „Belegung planen“ und konnte deren Klicks abfangen. — behoben in SessionId 43572 durch die Verlagerung der Demodaten-Steuerung in die Topbar; ein Playwright-Test stellt sicher, dass sie keine Dashboard-Aktion überlappt und „Belegung planen“ erreichbar bleibt.
[x] Reservierung anlegen hat eine Dropbox für Kunden. Das macht aber keinen Sinn. Das muss ein Suchfeld für Kunden sein. Autocomplete — durch zugängliche Kund:innen-Autovervollständigung mit Namens-/Telefonfilter, Maus- und Tastaturauswahl in Buchungen sowie Belegungs-Reservierung behoben in SessionId 40140
[x] Reservierung anlegen sollte die möglichkeit haben, mehrere tiere gleichzeitig zu reservieren. — gemeinsame Reservierungs-Entität mit verknüpften Einzelaufenthalten, atomarer Mehrtier-Kapazitätsprüfung und Mehrfachauswahl in Buchungen sowie Belegung umgesetzt in SessionId 68426.
[x] Das Symbol bei "Schließzeiten hinterlegen" ist verwirrend. Sieht aus wie Abbruch. Besser ein schloss oder ähnliches. — behoben in SessionId 4aca4 durch ein eindeutiges Schloss-Symbol.
[x] Bestehdende Kundschaft soll nur matches anzeigen. Und es muss klar sein, dass es zugeordet wurde — behoben in SessionId 4aca4: Die Auswahlliste enthält ausschließlich Namens-/Telefon-Treffer und erläutert die Zuordnung zur ausgewählten Bestandskundschaft.
[x] Die Zimmerauswahl bei Anfragen berücksichtigte hinterlegte Schließzeiten nicht. Dadurch blieb ein Zimmer auswählbar, obwohl die Annahme der Anfrage später an derselben Schließzeit scheiterte — behoben in SessionId 6adbf durch die gemeinsame Schließzeitenprüfung in der Vorauswahl; mit einem Playwright-Regressionstest abgesichert.
[x] Schau dir bug-screenshots/screen2.png an. Button ist zu klein — Der „Heute“-Button in der Datumsnavigation der Belegung hat jetzt eine klar erkennbare Mindestbreite und eine 40px-Klickfläche; die benachbarten Datumsnavigationselemente folgen derselben Höhe. Behoben in SessionId 62c6e
[x] Die globale Suche war als einfaches Textfeld nur per Platzhalter und Tastaturkürzel erkennbar. Für Screenreader wurde ihr Name aus der Tastenanzeige „⌘ K“ abgeleitet, statt ihre Aufgabe zu benennen; dadurch war sie in der Bedienung per Assistenztechnik nicht eindeutig auffindbar. — behoben in SessionId 31fb3 durch das semantische Suchfeld mit dem zugänglichen Namen „Globale Suche“; mit Playwright abgesichert.
[x] Die Abstände auf der Konto Seite sind nicht ganz korrekt — der Aktionsbereich der Kontodaten und die Kündigungszone sind jetzt durch einen klaren, responsiven vertikalen Abstand getrennt; mit einem Desktop-Playwright-Regressionstest abgesichert, behoben in SessionId 98f20
[x] Die Seitennummerierung im mobilen Kundenverzeichnis rendert für alle 20 Seiten gleichzeitig. Dadurch lief sie horizontal aus der Karte und die letzten Seitennummern waren abgeschnitten. — behoben in SessionId fc033 durch ein Fenster aus höchstens fünf Seitennummern um die aktuelle Seite; Vor-/Zurück-Navigation bleibt erhalten und ein mobiler Playwright-Regressionstest stellt sicher, dass die Navigation nicht überläuft.
[x] Screen3.png. Fixe abgeschnittenen Dropdown — Die Kund:innen-Autovervollständigung im Buchungsformular wurde durch das `overflow: hidden` der umgebenden Karte abgeschnitten. Die Formular-Karte lässt überlagernde Vorschläge nun gezielt sichtbar und liegt mit eigenem Stacking-Kontext über der folgenden Liste; Regressionstest ergänzt, behoben in SessionId 6b975.
[x] Neue Buchung, Kunde. Nicht sofort werte anzeigen, erst nach suche von 2 chars — Die Kund:innen-Autovervollständigung der neuen Buchung zeigt erst ab zwei eingegebenen Zeichen Treffer; davor erklärt ein zugänglicher Hinweis den nächsten Schritt. Behoben in SessionId f120d.
[x] bug-screenshots/screen4.png. Layout ist extrem unruhig. Sauber überarbeiten! — behoben in SessionId faf27: Jede offene Anfrage hat nun eine klar gegliederte Zusammenfassung mit Tier, Kundschaft, Aufenthalt und Verfügbarkeit sowie eine optisch abgegrenzte Entscheidungszone für Zuordnung, Zimmerwahl und Aktionen. Auf kleinen Viewports stehen die Bereiche ruhig untereinander; auf Desktop bleiben sie getrennt nebeneinander. Ein Playwright-Regressionstest sichert Gruppierung, Anordnung und fehlenden horizontalen Überlauf auf Desktop und Mobile.
[ ] Primary buttons sind viel zu dunkel
[ ] Tier Badges in Kunden & Tiere sehen komisch aus